 # This is an existing list of numbers hidden in the hat.

# Step 1: write a line of code that prompts the user
# to replace the middle number with an integer number entered by the user.

# Step 2: write a line of code that removes the last element from the list.

# Step 3: write a line of code that prints the length of the existing list.


number = (input(" Enter a number? ")) #Enters number

hat_list = [1, 2, 3, 4, 5] # hat l;ist

hat_list [len(hat_list)//2] =number # replace the middle item with number

del hat_list [-1] # del the last item

print (hat_list, "list length is:",len(hat_list)) # prints the hat list and list length
